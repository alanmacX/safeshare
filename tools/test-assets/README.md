# Local test assets

These files are development-only inputs for manual device regression. They
live outside `entry/src/main` so they cannot be packaged into AppGallery
release artifacts. Import them with the system picker or `hdc file send` when
running a local test; never copy them back into production resources.
